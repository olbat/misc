# jail.toml — Sandbox profiles for AI coding agents
#
# Each profile defines:
#   ro_paths          - host paths mounted read-only
#   rw_paths          - host paths mounted read-write (changes persist)
#   allowed_commands  - binaries whitelisted into sandbox /usr/bin
#   env               - environment variables ("$VAR" = pass through from host)
#   tmpfs             - tmpfs mounts (tables with path and optional size fields)
#   symlinks          - symlinks to create inside the sandbox
#   share_net         - allow network access (default: false)
#   new_session       - create a new terminal session (default: true)
#   die_with_parent   - kill sandbox when parent exits (default: true)
#   hostname          - custom hostname inside the sandbox
#   bwrap             - sandbox backend: true = bubblewrap (default), false = Landlock
#
# Paths support ~ for home directory expansion.
#
# IMPORTANT: Do NOT mount /usr or /usr/bin as ro_paths — this exposes
# every binary on the host and makes allowed_commands meaningless.
# Mount /usr/lib (shared libraries) and specific /usr/share subdirs instead.

# ---------------------------------------------------------------------------
# Profile: shell
# ---------------------------------------------------------------------------
[profiles.shell]
# Minimal profile for interactive testing — no agent, no network
ro_paths = [
  "/usr/lib",
  "/lib",
  "/lib64",
  "/usr/share/terminfo",      # terminal definitions for interactive use
]
rw_paths = []
allowed_commands = [
  "bash", "sh", "env",
  "ls", "cat", "head", "tail", "grep", "find",
  "sed", "awk", "sort", "uniq", "wc", "cut", "tr", "tee", "xargs",
  "echo", "printf",
  "cp", "mv", "mkdir", "rm", "touch", "chmod",
  "stat", "file", "diff",
  "dirname", "basename", "readlink",
  "date", "id", "whoami", "uname",
  "true", "false", "test", "pwd",
]
share_net = false
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.shell.env]
HOME = "/tmp"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.shell.symlinks]
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: claude
# ---------------------------------------------------------------------------
[profiles.claude]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.claude",        # agent state (history, cache, settings)
  "~/.claude.json",   # agent config
]
allowed_commands = [
  "claude",
  "git", "node", "python3", "npm", "npx",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs", "tee", "tr",
  "plutil",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.claude.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.claude.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: aider
# ---------------------------------------------------------------------------
[profiles.aider]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.config/aider",      # agent config
  "~/.aider.conf.yml",    # agent config (alt location)
]
allowed_commands = [
  "aider",
  "git", "python3", "pip",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.aider.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.aider.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: codex
# ---------------------------------------------------------------------------
[profiles.codex]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.codex",   # agent state
]
allowed_commands = [
  "codex",
  "git", "node", "python3", "npm", "npx",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs", "tee", "tr",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.codex.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.codex.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: cline
# ---------------------------------------------------------------------------
[profiles.cline]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.cline",   # agent state
]
allowed_commands = [
  "cline",
  "git", "node", "python3", "npm", "npx",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs", "tee", "tr",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.cline.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.cline.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: goose
# ---------------------------------------------------------------------------
[profiles.goose]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.config/goose",   # agent config/state
]
allowed_commands = [
  "goose",
  "git", "python3",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.goose.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.goose.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: continue
# ---------------------------------------------------------------------------
[profiles.continue]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.continue",   # agent state
]
allowed_commands = [
  "continue",
  "git", "node", "python3", "npm", "npx",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs", "tee", "tr",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.continue.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.continue.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"



# ---------------------------------------------------------------------------
# Profile: openhands
# ---------------------------------------------------------------------------
[profiles.openhands]
ro_paths = [
  "/usr/lib",
  "/usr/share/ca-certificates",
  "/lib",
  "/lib64",
  "/etc/resolv.conf",
  "/etc/ssl",
  "/etc/ca-certificates",
]
rw_paths = [
  "~/.openhands",   # agent state
]
allowed_commands = [
  "openhands",
  "git", "python3",
  "grep", "find", "cat", "ls",
  "cp", "mv", "mkdir", "rm", "sed",
  "head", "tail", "sort", "wc", "diff", "touch",
  "bash", "env", "dirname", "basename", "readlink", "xargs",
]
share_net = true
new_session = true
die_with_parent = true
hostname = "sandbox"
tmpfs = [
  {path = "/tmp", size = 67108864},  # 64 MiB
]

[profiles.openhands.env]
HOME = "~"
TERM = "$TERM"
PATH = "/usr/bin:/usr/local/bin"

[profiles.openhands.symlinks]
"/usr/bin/python" = "/usr/bin/python3"
"/usr/bin/sh" = "/usr/bin/bash"
